// Стъпка 61 — validators.js
// TODO: Централизирана валидация на форми
//
// В момента валидацията е вградена директно в отделните view файлове:
// - loginView.js: проверка за непразни полета
// - registerView.js: проверка за непразни полета, съвпадение на пароли
// - testEntryView.js: проверка за непразно ime, максимална дължина 100 символа
// - stepTitleView.js: проверка за непразно заглавие
// - stepCategoriesView.js: проверка за поне 1 избрана категория
// - stepQuestionsView.js: проверка за поне 1 въпрос с поне 1 верен отговор
// - categoriesView.js: проверка за непразно категорийно ime, максимална дължина 100 символа
//
// Дублирани модели за повторна употреба:
// - isNonEmpty(value): проверява дали стойността не е празна след trim()
// - maxLength(value, max): проверява дали дължината е в рамките на лимита
// - isValidEmail(value): проверява email формат с прост regex
//
// Рефакторингът на валидацията тук е отложен за бъдеща итерация,
// тъй като всяка форма има уникална структура и специфично поведение при грешки.
